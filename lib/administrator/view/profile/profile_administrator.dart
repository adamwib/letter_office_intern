import 'package:flutter/material.dart';

class ProfileAdministrator extends StatelessWidget {
  const ProfileAdministrator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}